# 苍云器灵：部署与运维手册

更新日期：2026-08-25

## 当前状态

旧公网部署已经停用，其地址、登录主密码和 frp 共享密钥均视为退役信息，不得重新使用。

仓库继续保留两种公网发布能力：

1. **Cloudflare Quick Tunnel**：适合临时演示，自动获得临时 HTTPS 地址；
2. **自建 VPS + frp**：适合国内访问，但必须在公网入口增加 HTTPS 反向代理。

两种方式都使用 Router 模式：入口进程负责鉴权和反向代理，每个用户名拥有独立 worker 与用户目录。修改部署主密码不会删除成员、个人密码、现有会话 token 或用户数据。

真实秘密只能由环境变量、秘密管理服务或仓库外私有配置注入。本文、发布脚本和 Git 仓库不得保存真实值。

## 目录与端口

| 项目 | 默认值 | 说明 |
|---|---:|---|
| 本地单进程入口 | `127.0.0.1:3005` | 普通开发与本机体验；后端同时托管前端 |
| frp Router 入口 | `127.0.0.1:3006` | 与本地调试端口错开 |
| frps 控制端口 | `7000` | frpc 与 frps 通信；可通过环境变量修改 |
| frp origin 端口 | `2014` | 只作为 HTTPS 反向代理的源站，不直接分享 |
| 用户数据 | `backend/userdata/` | 本机私有状态，已被 `.gitignore` 排除 |

端口都可以通过环境或私有配置修改。公开文档不要写真实 VPS 地址。

## 0. 构建与本地验证

在项目根目录执行：

```powershell
cargo test --manifest-path backend/Cargo.toml
cargo build --release --manifest-path backend/Cargo.toml
```

发布前先验证本地服务：

```powershell
Set-Location backend
cargo run
```

访问 `http://localhost:3005`，并检查：

```powershell
Invoke-WebRequest http://127.0.0.1:3005/health -UseBasicParsing
```

## 1. 准备新的部署主密码

`JX3_AUTH_PASSWORD` 是“创建新用户名”的门槛，不是数据库加密密钥。已有白名单成员仍按自己的个人密码规则登录；已经签发的会话 token 保持有效。

在即将运行发布脚本的 PowerShell 窗口中安全输入：

```powershell
$secure = Read-Host 'Deployment master password' -AsSecureString
$env:JX3_AUTH_PASSWORD = [Net.NetworkCredential]::new('', $secure).Password
```

要求：

- 每次新部署使用新的高熵值；
- 不把值写进 `.ps1`、`.env.example`、截图或聊天记录；
- 不在脚本中打印；
- 公网演示结束后关闭入口。

## 2. 临时演示：Cloudflare Quick Tunnel

### 准备

1. 从 Cloudflare 官方渠道下载 `cloudflared`；
2. 放到本机 `tools/cloudflared.exe`；
3. 完成 release 构建和本地健康检查；
4. 在当前 PowerShell 会话设置新的 `JX3_AUTH_PASSWORD`。

下载的二进制不进入 Git，发布时应核对来源和校验值。

### 启动

```powershell
powershell -ExecutionPolicy Bypass -File .\publish.ps1
```

脚本会：

1. 确认部署主密码存在且长度不少于 12；
2. 启动 Router；
3. 等待 `/health` 成功；
4. 启动 Quick Tunnel 并由 cloudflared 输出临时 HTTPS 地址。

脚本不会打印登录主密码。如果 Router 未通过健康检查，脚本不会继续开放隧道。

### 停止

在 tunnel 窗口按 `Ctrl+C`，再关闭 Router 窗口。确认公网地址已经不可访问。

Quick Tunnel 适合短时间面试演示，不等于正式生产部署，也不提供固定域名或 SLA。

## 3. 固定演示：VPS + frp

### 3.1 生成全新的 frp 密钥

不要复用退役密钥。使用密码管理器或密码学安全随机源生成至少 32 字节随机值。密钥只在 frpc 与 frps 两端保存。

### 3.2 配置 VPS 上的 frps

将 `vps_setup_frps.sh` 复制到 VPS，然后在 root shell 中安全输入密钥：

```bash
read -rsp 'New frp token: ' FRP_AUTH_TOKEN && echo
export FRP_AUTH_TOKEN
bash vps_setup_frps.sh
unset FRP_AUTH_TOKEN
```

可选环境变量：

- `FRP_VERSION`
- `FRP_BIND_PORT`
- `FRP_SERVICE_PORT`

脚本会备份已有 `/opt/frp` 目录、安装 frps、写入权限为 `600` 的私有配置并启动 systemd 服务。它不会把 token 打印到终端。

检查：

```bash
systemctl is-active frps
systemctl --no-pager status frps
ss -ltnp
```

不要执行会把完整 `frps.toml` 打印到共享日志或录屏中的命令。

### 3.3 配置本机 frpc

复制样例：

```powershell
Copy-Item .\tools\frpc.example.toml .\tools\frpc.toml
```

只在被 Git 忽略的 `tools/frpc.toml` 中填写：

- VPS 地址；
- frps 控制端口；
- 与 VPS 一致的新共享密钥；
- Router 本地端口和远端 origin 端口。

确认以下文件不会进入 Git：

```powershell
git check-ignore -v tools/frpc.toml
```

`tools/frpc.example.toml` 使用标准保留地址和无效占位密钥，可以公开。

### 3.4 配置 HTTPS

frp 的 origin 端口不应作为作品集公网地址直接分享。应在 VPS 上使用 Caddy、Nginx 或同类反向代理：

- 域名解析到 VPS；
- 自动申请并续期 TLS 证书；
- HTTPS 入口反代到 frp origin；
- origin 端口通过主机/云防火墙限制暴露范围；
- 只向用户分享 `https://` 地址。

反向代理配置取决于域名和服务器环境，因此不在公开仓库硬编码。

### 3.5 启动本机 Router 与 frpc

先设置新的 `JX3_AUTH_PASSWORD`，再执行：

```powershell
$env:JX3_PUBLIC_URL = 'https://your-domain.example'
powershell -ExecutionPolicy Bypass -File .\publish-frp.ps1
```

`JX3_PUBLIC_URL` 只用于显示提示，可以省略。脚本会拒绝仍含文档占位符的 `frpc.toml`，并且不会打印任何秘密。

### 3.6 停止

在 frpc 窗口按 `Ctrl+C` 并关闭 Router。长期停止服务器端服务时执行：

```bash
systemctl disable --now frps
```

重新开放前必须重新检查密钥、HTTPS、防火墙和应用版本。

## 4. 用户数据与会话保留

以下数据必须原样保留在本机：

- `backend/userdata/whitelist.json`：成员、个人密码摘要和会话 token；
- `backend/userdata/users/`：每个用户的配置、运行状态与产物。

这些内容已经被 `.gitignore` 整体排除。不要为了发布仓库而删除、移动或提交其中的文件。

轮换 `JX3_AUTH_PASSWORD` 不会使现有会话失效；它只改变创建新用户名时需要提供的主密码。如果未来有证据表明 `whitelist.json` 曾被公开，再单独评估是否需要撤销 token。

备份时使用访问受控的私有介质，恢复后保持目录权限，不把备份压缩包放入项目目录。

## 5. 健康检查与故障定位

### Router 无法启动

- 检查 release 可执行文件是否存在；
- 检查 3005/3006 是否被占用；
- Router 模式必须设置非空 `JX3_AUTH_PASSWORD`；
- 工作目录应允许访问 `backend/data`、`frontend` 和私有 userdata。

### 页面可开但无法登录

- 新用户名使用本次部署主密码；
- 已有用户名有个人密码时使用个人密码；
- 已有用户名没有个人密码时保持原有免密规则；
- 不要通过日志输出请求体排查密码问题。

### frpc 无法连接

- 检查 frps 服务状态和控制端口；
- 确认双端使用同一个新 token；
- 检查 TLS 与 `tcpMux` 设置一致；
- 检查云安全组和本机防火墙；
- 不要把完整配置粘贴到公开 Issue。

### 公网慢或不稳定

- Quick Tunnel 的临时链路不保证固定质量；
- frp 方案检查 VPS 地域、带宽、TLS 反代和进程资源；
- 使用 `/health` 区分入口问题与模拟任务问题；
- 性能日志必须脱敏，避免记录用户标识和凭据。

## 6. 发布安全清单

- [ ] 使用本次部署新生成的登录主密码；
- [ ] frp 使用本次部署新生成的双端密钥；
- [ ] 真实 `.env`、`frpc.toml` 和 userdata 被 Git 忽略；
- [ ] README、脚本和截图不含真实地址或秘密；
- [ ] release 构建、单元测试、golden 与 smoke 通过；
- [ ] Router 健康检查通过后才开放隧道；
- [ ] 固定公网入口使用 HTTPS；
- [ ] 防火墙只开放必要端口；
- [ ] 演示结束后关闭不再使用的入口；
- [ ] 日志没有输出密码、token、Cookie 或完整私有配置。

更完整的仓库发布门槛见 `docs/RELEASE_CHECKLIST.md`（第 0 阶段后续创建）。

