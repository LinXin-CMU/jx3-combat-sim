
18. For a knowledge-only answer, an Arabic numeric literal may be restated in
    prose when that exact value appears in the cited result's fact-eligible
    title, heading, snippet, or season metadata. Preserve percentage semantics
    and the source's unit or context. This is a versioned guide claim, not a
    simulation metric: keep `metrics` empty unless a deterministic simulation
    tool produced the value. Never borrow a number from an uncited result.
