# Agent 真实模型基线：DeepSeek V4 Pro

日期：2026-08-26

模型：`deepseek-v4-pro`

协议：DeepSeek Chat Completions 兼容层，显式关闭 thinking

公网：关闭；仅由本机 `127.0.0.1:3017` 隔离进程执行评测

## 凭据与费用边界

API key 只保存在用户级环境变量 `JX3_DEEPSEEK_API_KEY`。仓库配置仅保存环境变量名，安全启动脚本只把 key 注入子进程；浏览器、会话、trace、日志和 Git 都不保存 key。

开发评测硬上限为 USD 0.70。成本按官方 cache-miss 输入 USD 0.435/M token、输出 USD 0.87/M token保守计算。当前可计量调用合计约 USD 0.161840；另有少量失败请求没有返回 usage，因此不把这个数字表述为账单精确值。

## 固定十题结果

题集包含六个自然语言策划问题、两个提示注入和两个授权边界问题。成功标准由脚本判定，不靠人工挑选漂亮回答；原始运行报告保存在被 Git 忽略的 `backend/runs/`，避免提交真实会话内容。

| 迭代 | 通过 | tokens | 估算费用 | p50 / p95 | 未计量失败 | 越权工具 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 初始兼容层 | 1/10 | 73,107 | USD 0.033827 | 3,121 / 18,000 ms | 4 | 0 |
| 协议与提示词修复后 | 6/10 | 83,255 | USD 0.042662 | 19,058 / 37,043 ms | 0 | 0 |
| 引用归一化复跑 | 5/10 | 100,811 | USD 0.050182 | 19,760 / 30,285 ms | 0 | 0 |

第二次和第三次结果不同，说明在温度不可控的兼容接口上仍存在输出波动，因此不把最好一次包装成稳定准确率。可以稳定宣称的是：请求已全部可计量完成，暴露给模型的写工具为零，三轮评测的越权工具调用均为零。

## 发现与修复

- DeepSeek 的工具轮次与 JSON mode 组合可能返回空白内容：有工具时不再发送 `response_format`，最终无工具修复轮次才启用 JSON object。
- 复杂工具 transcript 上直接做报告修复会触发上游请求错误：修复改为独立、无工具、只包含被拒可见 JSON 的新请求。
- 模型经常把 metric 的 evidence id 放在 metric 内，却漏掉 finding 外层引用：服务端只对“本轮已存在的同一 evidence id”做确定性补链，然后继续执行数值、单位与 JSON Pointer 校验；未知或数值不匹配仍拒绝。
- 真实失败主要集中在 `missing_metric_source` 与 `metric_value_mismatch`。安全回退为 `evidence_insufficient`，未经验证的数字不会进入报告。

## 真实多轮会话

发现原实现只保存 `parent_run_id`，并未把上一轮可见结论送入下一轮模型上下文。现已改为从 append-only 会话中重建最近两轮的有界可见报告；不包含 provider transcript、隐藏推理或凭据，历史数字也不能充当本轮证据。

真实续接 run `run-1a03bfb043b-0` 在既有 session `session-1a03be31db5-0` 上完成：模型识别上一轮使用英文分析确定性基线，按当前中文问题重新调用 `get_current_scenario` 与 `simulate_scenario`，生成一份当前轮 evidence 支撑的报告。该轮 10,409 tokens、约 USD 0.005298、20,983 ms。

## UI 真实链路验收

在前端整合提交 `6627a0e` 上，使用本机 `127.0.0.1:3017` 安全实例完成了两轮真实验收。两轮均追加到同一个持久会话，未删除或覆盖原有会话。

| 轮次 | 入口 | 结果 | 工具 / 模拟 | tokens | 耗时 |
| --- | --- | --- | ---: | ---: | ---: |
| 1 | Run API 与完整 Agent 页恢复 | `completed`，2 条有效证据 | 2 / 1 | 7,844 | 24,385 ms |
| 2 | 循环模拟页 AI 悬浮窗续接 | `evidence_insufficient`，`missing_metric_source` 安全降级 | 3 / 2 | 15,966 | 30,511 ms |

第二轮通过页面发起，SSE 展示 11 个规范化过程步骤，调用 `get_current_scenario`、`analyze_timeline` 和 `simulate_scenario`。模型给出的数值来源未通过服务端校验，因此页面只展示“证据不足”，没有将未验证数字包装为结论。点击“展开”后，完整 Agent 页恢复 2 条用户消息、2 组轨迹和 2 张报告；历史列表保持内部滚动，输入区仍在单屏内可见。

本次合计 19,044 输入 tokens、4,766 输出 tokens，按上文价格口径估算约 USD 0.012431，低于用户批准的 5 元人民币上限。会话 API 对 `Authorization`、API key 环境变量名、provider 原始请求/响应与隐藏推理标记复检为 0 命中。

## 可读数值与 Prompt v2

UI 验收暴露出 v1 的过度约束：正文禁止所有阿拉伯数字后，模型会把精确值改写为中文数字，可读性差且不增加真正的安全性。当前 `agent-system/v2` 改为：

- 小整数的普通计数可直接出现；
- 精确值可用阿拉伯数字复述已验证 metric；
- 允许正常四舍五入、千分位和百分比显示；
- 科学计数法与不匹配 metric 的数值仍拒绝。

新规则通过 Agent 84/84 回归和隔离 HTTP Run/Session/SSE smoke，实例报告为 `agent-system/v2`，prompt SHA-256 为 `43a7ab4d848c1b0793313d774e78e3cb2b08ce056aba6d642a17592c6d72e484`。本节未新增真实模型费用。

## 当前结论

Phase 2 的工程闭环和真实部署链路已成立，但真实模型固定题集尚未达到可称为高可靠产品的门槛。作品集应展示“证据约束、失败降级、成本治理、跨供应商兼容和诚实评测”，而不是宣称模型能稳定回答所有策划问题。

上述三轮 DeepSeek 固定评测使用的 Prompt SHA-256：`080b1ccfce57e0730c83034ad56a815cf6771d4fdb0fa782116efbd8abd5f2c1`。当前 v2 SHA-256 见上文。
