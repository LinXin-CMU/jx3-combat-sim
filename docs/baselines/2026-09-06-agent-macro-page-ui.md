# Agent 宏分页展示

- 分体态宏按盾宏、刀宏分别显示；兼容中文分页、盾墙页和通用单页。
- 每页独立边框、正文 UTF-16 字数（含换行、上限 128）和复制按钮。分页复制只含正文，顶部“复制完整宏”保留原始文本及 `#page` 标记；报告复制保持完整交付内容。
- 长命令保持一行，仅代码块内部横向滚动。复用现有主题颜色，超限使用主题红色。
- 未知分页和普通代码原样展示，全部使用 textContent，避免吞掉无效文本或执行 HTML。
- 前端静态资源版本更新，3005 刷新即生效，未改后端、宏或会话数据。

验证：`node --check frontend/agent.js`、`node --check frontend/app.js`；`python tools/agent-artifact-ui-smoke.py` 覆盖独立页/悬浮窗、分页及完整复制、报告复制、长行、中文分页、CRLF、无效分页、普通代码转义和深色/米白/樱花主题，均通过。

模型修改后再次调优的入口已经存在：`distill_macro({macro_text: 完整修订宏})`，以传入候选接续搜索。v49 Skill 已明确此用法，上轮真实验收完成“初稿调优 → 模型修改与对比 → 再次算法调优 → 最终对比”，详见 v49 修复记录。本轮无需另建优化器或改变后端行为。
