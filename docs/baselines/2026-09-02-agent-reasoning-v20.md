# Agent Reasoning v20 验收基线（2026-09-02）

## 固定场景

- 场景：`e4f817e6ebee2a3c1d28166cbe0504c72f4f4c12fd1ea2bf31dcbdeedc128e66`
- 版本 / 心法：暗影千机 / 分山劲（旗舰端）
- 输入：可解析的一键宏，300 秒，60 ms 网络延迟
- 模型：`deepseek-v4-pro`

## 只诊断、不改宏

- run：`run-1a05fe8076d-0`
- prompt：`agent-system/v20`
- 结果：未提出推荐或宏改动；分别说明输出画像、已验证优点、可观察风险与证据边界。
- 因果边界：把 54 次怒气触顶表述为待验证信号，没有直接换算成怒气或 DPS 损失。
- 轨迹：先读取场景与当前资料，再运行时间轴；发布前语义批判通过。

## 诊断后优化

- run：`run-1a05fed12ac-0`
- prompt：`agent-system/v20 / f6896ec29a460f82912db4a3bbf37b166acca965298547845526195bf0c1e800`
- 结果：先完成时间轴画像，再提出一个阵云判定候选，并执行同场景单变量对比。
- 决策：候选未改善基线，因此不发布宏改动并保留原宏。
- 轨迹：`baseline → hypothesis → experiment → publish → critique`；比较证据已进入最终建议。

## 确定性回归

- Rust：225 项通过（含推导状态、干预边界、因果强度、已完成实验不得重复、比较结论降级摘要）。
- 工具题集：20 / 20，通过且无写入尝试，用户数据未变化。
- 会话接口：创建、轮询、SSE 回放、持久化恢复、终态取消与不存在 ID 均通过。
- 前端：`agent.js` 语法检查通过。

## 仍需继续观察

- DeepSeek 偶尔会在比较结束后重复请求一次已缓存的当前场景；不会重新模拟或改变结论，但会增加一次模型往返。
- 模型偶尔在 JSON 外加说明或携带过多数字；服务端会聚焦内容，并从同场景比较证据生成已完成实验的决策摘要。
- v20 展示的是阶段、判断、证据与批判摘要，不展示供应商私有逐 token 思维。
