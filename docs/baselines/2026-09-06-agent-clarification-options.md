# 等待回答：可选择的追问

- 前端共享选项组件用于独立分析页与循环/配装浮窗，使用现有主题变量和 `sim-btn`。
- 编号单选，确认后才提交；另有自由填写入口，无默认代选。保留主输入框草稿。
- 回答包含原追问与用户所选答案，沿用当前 session；提交期间禁用选项与重复发送。历史已被后续 run 替代的追问禁用。
- 旧记录的 `answer_hint` 仅在具有明确的空格分隔 `/`、`／`、`|`、`｜` 时拆为 2–4 项；其他情况显示原提示与自由输入。
- 后端 `ask_user_question` 新增可选 `options: [{label, description}]`，支持 2–4 项。兼容原会话反序列化；选项随 clarification 保存、续轮上下文及反馈包呈现。
- `node --check frontend/agent.js` 通过；`cargo check --tests` 编译检查通过。
- `python tools/agent-clarification-ui-smoke.py` 使用无头 Edge，拦截任务创建，验证旧提示兼容、结构化选项、两个界面、选中不提交、确认提交、自由输入、原 session 和草稿保留；3 次请求全部拦截，真实模型调用 0，userdata 写入 0。
- 本次前端由 3005 静态目录直接提供，刷新即生效；未重启服务。后端结构化选项协议需要下一次构建部署后生效。新增 Rust 选项回归用例已写入，未执行完整测试套件。
