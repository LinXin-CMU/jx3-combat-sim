# 回复排版与技能状态浮窗

- 修复此前纯文本直接展示 Markdown 标题、加粗和表格的问题。使用 DOM 文本节点渲染，模型文本不作为 HTML 执行；表格内部滚动，时间引用保持可交互。
- 技能摘要改为图标加名称。技能按钮的悬停/键盘聚焦显示释放后状态，前方间隔显示该技能释放前状态；浮窗内展示怒气、消耗、盾值、伤害及自身/目标 Buff，Buff 展示层数与剩余秒数。
- 直接使用现有 `state_before` / `state_after` 快照，不根据后续技能倒推状态；缺少快照时明确提示。自身团辅过滤跟随主技能轴设置。图标复用现有本地代理与 helper。
- 浮窗按可用空间选边和限制高度，避免覆盖触发链接；原时间轴标记与居中逻辑不变。
- JavaScript 语法检查通过；合成会话与时间轴桥接的浏览器测试覆盖 Markdown 表格/加粗、时间链接、多位置、技能图标、释放前后怒气、Buff 展示与定位回调。等待回答在大页/侧栏的回归检查通过。
- 仅修改前端静态文件，当前 3005 服务可直接读取；无需重启或写入既有会话。本次没有付费模型调用，没有修改战斗计算。
