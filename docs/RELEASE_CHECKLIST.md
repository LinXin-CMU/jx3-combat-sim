# 作品集发布检查表

适用范围：创建公开 Git 远端、发送公开仓库链接、开启公网 Demo 或交付招聘材料之前。

规则：任一“阻断项”未完成，都不能标记为可公开发布。

## 当前 Phase 0 状态（2026-08-25）

**发布结论：阻断。** 两版本 Golden v2 已批准并通过；当前仍有 3 类直接状态写入违规。旧部署已停用，外部旧凭据失效状态无法在线验证。代码与本地数据没有因此被删除，现有部署功能保留为参数化模板。

## 1. 安全与隐私（阻断项）

- [x] 真实部署值已从纳入 Git 的源码、脚本和文档中移除，配置改为环境变量或 example。
- [x] `backend/userdata/` 整体忽略，本机现有成员与会话数据保留且未提交。
- [x] 工作区秘密扫描无高置信命中；结果见 `docs/security/SECRET_SCAN_REPORT.md`。
- [ ] 公开部署前已生成全新的应用密码和 frp 双端密钥，并确认未复用旧值。
- [ ] 若恢复旧 VPS，已在服务端替换/撤销旧凭据并验证旧值无法连接。
- [ ] 截图、GIF、视频和日志中不存在用户名、公网地址、Cookie、令牌或本机绝对路径。
- [ ] 公网 Demo 使用 HTTPS、强鉴权、最小权限和合理的请求/资源限制。

## 2. 仓库边界（阻断项）

- [x] `.gitignore` 覆盖构建物、用户数据、模型权重、训练日志、真实配置与本机缓存。
- [x] 首个安全基线提交已创建：`f4c9e13`。
- [ ] `git status --short` 为空，待发布提交均已审阅。
- [ ] 对 `git ls-files` 完成最终秘密扫描和大文件检查。
- [ ] README、文档和脚本中的相对链接全部有效。
- [ ] 已决定许可证策略；未授权复用时不添加暗示开放许可的文本。

## 3. 功能与回归（阻断项）

- [x] `cargo test`：34 通过、0 失败。
- [x] `node --check frontend/app.js`：通过。
- [ ] `bash backend/tests/check_no_direct_writes.sh`：当前失败，须处理 3 类违规。
- [x] 两版本共 8/8 固定场景满足重复运行确定性。
- [x] 两版本共 8/8 固定场景满足 Lite/Full 完整数值等价。
- [x] 两版本共 8/8 Golden v2 匹配；生成提交、场景哈希和数据哈希均已记录。
- [x] seed 42、30 个宏 fuzz 场景全部通过。
- [x] `/health`、首页和固定模拟 smoke 全部通过。
- [x] Release 性能基线已记录硬件、输入、样本数、P50 与 P95。

详细证据见 `docs/baselines/2026-08-25-phase0.md`。

## 4. 作品集表达（阻断项）

- [x] README 明确区分“已实现”与“Agent 路线图”，没有把规划能力写成成品。
- [x] README 能说明项目定位、架构、运行、验证、个人职责和已知限制。
- [ ] 至少准备一组可公开截图或短演示，并按 `docs/portfolio/ASSET_CHECKLIST.md` 完成隐私检查。
- [ ] 至少准备一个“策划问题 → 模拟/Agent 方法 → 可核验证据 → 设计结论”的案例。
- [ ] 所有性能、准确率和效率数字都能追溯到提交、输入与运行命令。
- [ ] 非官方项目、游戏素材归属和数据来源边界表述清楚。

## 5. 发布记录与回滚（阻断项）

- [ ] 标记待发布提交或版本号，并记录变更摘要。
- [ ] 记录可回滚提交；数据库/用户数据变更另有备份与回滚说明。
- [ ] 已知限制与未通过项公开可见，不使用模糊措辞隐藏失败。
- [ ] 在全新目录按 README 完成一次从克隆到 smoke 的复现。
- [ ] 发布后检查公开仓库文件列表与 Demo 健康状态。

## 签署

- 检查日期：`YYYY-MM-DD`
- 提交/版本：`<commit-or-tag>`
- 检查人：`<name>`
- 结论：`READY / BLOCKED`
- 阻断原因：`<none-or-list>`
