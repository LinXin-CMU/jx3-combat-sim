# 敏感资产与轮换清单

更新日期：2026-08-25  
状态：P0-01 至 P0-04 完成；旧公网部署与旧凭据已退役，参数化部署能力保留，公开候选文件扫描通过。

本文只记录秘密类型、所在位置和处理状态，不记录任何秘密值。真实值不得出现在提交、Issue、截图、终端日志或测试报告中。

## 轮换对象

| 编号 | 类型 | 当前暴露位置 | 风险 | 计划动作 | 当前状态 |
|---|---|---|---|---|---|
| S-01 | 公网登录主密码 | 两个发布脚本、旧运维手册 | 知道旧值的人可尝试创建账号 | 旧部署停用；脚本已改为环境变量注入，未来部署必须生成新值 | 已从公开文件移除 |
| S-02 | frp 双端共享密钥 | 私有客户端配置、旧 VPS 初始化脚本与旧运维手册 | 若旧服务仍开放，可尝试连接隧道服务 | 旧配置已删除；脚本已参数化，未来双端必须生成新值 | 已退役 |
| S-03 | 用户会话令牌 | 本地 whitelist 用户数据 | 泄露后可能冒用既有会话 | 原样保留在本机忽略目录；仅在发现外泄证据时另行确认撤销 | 保留，不轮换 |
| S-04 | 用户密码摘要与用户名 | 本地 whitelist 用户数据 | 属于个人/鉴权数据，不应公开 | 不复制、不样例化真实记录，目录整体忽略 | 已建立忽略边界 |
| S-05 | 公网地址与部署拓扑 | 运维手册、frp 配置、发布说明 | 扩大外部攻击面并暴露个人部署信息 | 公开文档改为保留地址和占位符 | 公开文件已清理 |
| S-06 | 服务器登录凭据 | 未在工作区发现私钥文件 | 若旧服务器仍开放，会留下外部攻击面 | 旧部署不再操作；重新部署时使用新认证和最小权限 | 无本地文件待处理 |
| S-07 | 未来模型 API Key | 当前未接入 | Agent 阶段可能新增高价值凭据 | 只允许环境变量/秘密管理，不写入场景或 trace | 规则已建立 |

## 不进入公开仓库的本机状态

| 路径/类型 | 盘点结果 | 处置 |
|---|---:|---|
| `backend/target/` | 约 5.5 GiB 构建产物 | 忽略，可重建 |
| `.venv/` | 约 4.3 GiB Python 环境 | 忽略，由 requirements 重建 |
| `backend/userdata/` | 6 个成员记录及用户运行状态 | 整体忽略，不逐文件筛选 |
| `backend/runs/` | 约 1600 个优化运行文件 | 忽略，后续只整理脱敏案例摘要 |
| `python/runs/`、`python/pretrains/` | 15 个模型权重及训练日志 | 忽略，保留复现说明而非权重 |
| `dist/` | 本地发布产物 | 忽略，由 release 流程重建 |
| `tools/*.exe` | 下载的隧道/部署二进制 | 忽略，README 记录官方获取方式与校验方法 |
| `.claude/` | 旧代理记忆、临时脚本和过期设计资料 | 作为本地历史档案忽略；稳定内容已迁移至 `AGENTS.md` 与 `.agents/skills/` |

规则数据中的大型装备表属于模拟器运行输入，不按生成物排除。当前 10 个大型数据表已完成定向秘密扫描，Git 全历史最大对象为 24.18 MiB；其数据来源与公开授权边界仍需在首次发布前确认。

## 可公开样例

- `.env.example`：只记录变量名和本地安全默认值；
- `tools/frpc.example.toml`：使用标准保留地址与占位密钥；
- `examples/whitelist.example.json`：只展示结构，不含真实用户和 token；
- 发布脚本：轮换后改成从环境变量读取，缺失时失败。

## 旧部署退役与未来部署验收

旧部署不再使用，因此本阶段不连接旧 VPS、不尝试复用旧值。不得粘贴秘密：

- [x] 公开发布脚本不再包含公网登录主密码；
- [x] VPS 安装脚本不再包含 frp 密钥；
- [x] 公开运维手册不再包含旧地址和凭据；
- [x] 本机旧 `tools/frpc.toml` 已删除；
- [x] 已有成员、个人密码摘要、会话令牌和用户运行数据原样保留；
- [x] 本机验证已有用户数据未被修改；
- [ ] 未来重新部署时生成新的登录主密码和 frp 双端密钥；
- [x] 执行记录没有在公开文档中记录秘密；
- [x] 当前公开候选文件扫描通过；
- [x] Git 首次暂存文件列表的二次扫描通过。

## 阻断规则

在秘密扫描未通过前：

- 不创建公开远端；
- 不执行首次全量暂存；
- 不发布仓库压缩包；
- 不把当前部署入口写入 README 或作品集。
