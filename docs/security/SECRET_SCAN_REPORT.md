# 第 0 阶段秘密扫描报告

扫描日期：2026-08-25  
扫描状态：通过  
扫描对象：按照当前 `.gitignore` 设计将进入公开仓库的源码、配置、规则数据、脚本与文档。

## 结果

| 检查 | 范围 | 结果 |
|---|---:|---:|
| 已知退役凭据字面量扫描 | 整个工作区，排除构建环境 | 0 命中 |
| detect-secrets 1.5.0 | 428 个公开文本文件 | 0 命中 |
| 大型装备 `.tab` 定向凭据扫描 | 6 个数据表 | 0 命中 |
| Git 首次暂存集合二次扫描 | 430 个文本文件 + 6 个大型数据表 | 0 命中 |
| PowerShell 语法 | 2 个发布脚本 | 通过 |
| Bash 语法 | VPS frps 安装脚本 | 通过 |

## 排除范围

以下内容按设计不进入公开仓库，也不属于本次公开文件扫描集合：

- Rust/Python 构建环境与缓存；
- RL 模型权重、训练日志和优化运行目录；
- `backend/userdata/` 中的成员、会话和用户状态；
- 本机下载的部署二进制；
- 已迁移完成的 `.claude/` 本地历史工作区；
- ZIP、PDF、图标等二进制文件不使用文本熵检测，其中拟公开的大文件仍需在 Git 首次暂存后核对文件列表和来源。

## 扫描方法

1. 使用固定规则检查密码、token、API key、Authorization、Bearer 和私钥头等高风险模式；
2. 使用 `detect-secrets` 的默认插件集并关闭网络验证，避免把候选内容发送到外部；
3. 对体积较大的数值表使用定向规则，避免通用熵扫描将大量游戏数值误判为秘密；
4. 扫描输出只记录文件、行号和规则类型，不输出候选秘密全文。

## 后续门槛

本报告不替代 Git 文件列表复核。初始化 Git 后还必须：

- 检查 `git status --short --ignored`；
- 对 `git ls-files` 或首次暂存文件集合再次扫描；
- 确认真实 `.env`、`frpc.toml`、userdata、模型和日志未进入暂存区；
- 公开远端启用托管平台的秘密扫描能力。
