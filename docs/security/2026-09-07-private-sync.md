# 私有 GitHub 同步检查

日期：2026-09-07。范围：现有私有仓库 main 的代码、测试、公共配置模板、脱敏项目文档与 README；不变更仓库可见性、不打发布标签、不修改运行中的部署。

## 提交内容安全

- tracked 文件与 untracked 候选合并检查；复扫时 614 个文本文件。
- detect-secrets 1.5.0 使用 `--no-verify`，未向服务商发送检测到的候选秘密。
- 17 个检测候选经核验均为误报：14 个已有指纹值，3 个环境变量名称。
- 另行核验测试中的合成 key 字面量、前端 token 词法比较及公开供应商 / 官方文档域名，均非实际凭据。
- 未发现真实部署 IP、私钥、用户数据、完整会话、运行日志或知识库全文进入提交候选。
- 未发现新增二进制或新增超过 5 MiB 文件；既有最大文件为 25,436,350 字节的游戏数据表，第三方权利仍按 THIRD_PARTY_NOTICES.md 说明，不扩展授权。
- `.gitignore` 对 userdata 和代表性私有路径生效；真实部署材料仍位于仓库外的私密目录。
- 简历素材仅含项目证据与措辞，没有个人身份 / 联系方式。

检查结论：允许同步到已存在的私有仓库。此结果不是安全漏洞不存在的证明，也不是公开发行 / 无认证互联网服务的批准。

## 快速验证

- `cargo test --quiet --target-dir target/agent-context-check`：337 passed，1 ignored，0 failed。
- `node --check frontend/app.js`、`node --check frontend/agent.js`：通过。
- 本轮未调用真实模型、未重跑长耗时调优或公网压力测试。
- 历史 golden 的 data_sha256 漂移保持记录，不改基准掩盖失败；现有工具 / 模型评测数字按各自基线日期引用。

## 简历发布口径

只将“单场景宏输出保留约 98.35%”作为限定案例；100 worker 仅为配置，未宣称百人并发实测。项目采用 AI 辅助实现，个人贡献与工作分工应按实际经历确认。
