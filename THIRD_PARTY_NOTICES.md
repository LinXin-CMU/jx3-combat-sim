# 第三方数据、素材与商标说明

## MIT 许可证范围

根目录 `LICENSE` 中的 MIT 许可证适用于本项目作者原创的程序源码、测试、构建/部署脚本和原创文档，文件另有声明的除外。

MIT 许可证不授予下列内容的版权、商标权或再分发许可：

- 《剑侠情缘网络版叁》及其名称、商标、技能/物品名称、机制描述、客户端数据和相关美术素材；
- 从游戏客户端导出或基于客户端表整理的规则/装备数据；
- 第三方项目、网站或数据服务提供的代码、数据、图标和文字；
- 各依赖包中由其权利人按独立许可证提供的内容。

上述权利归各自权利人所有。本项目是非官方、非商业的玩家研究与策划实验项目，与游戏官方及相关第三方不存在隶属、合作或背书关系。

## 当前数据来源边界

| 内容 | 本项目中的使用 | 来源/边界 |
|---|---|---|
| 游戏版本、技能、Buff、奇穴、秘籍和装备规则 | 确定性模拟输入与验证样例 | 由游戏内描述、客户端数据与人工核对整理；属于游戏相关事实/派生数据，不纳入 MIT 授权 |
| `backend/data/equip/` 原始表及 `backend/data/equip.json` | 配装查询与模拟输入 | 原始表来自本地客户端导出及用户提供的客户端表；公开再分发边界尚未确认，因此当前 GitHub 远端保持私有 |
| JX3BOX 图标信息 | 技能/Buff 图标 ID 与 `icon.jx3box.com` URL；运行时图标缓存位于未提交的 userdata | 参考 `JX3BOX/jx3box-data` 和 JX3BOX CDN；本仓库不提交运行时下载的图标文件，且不主张其授权 |
| jx3dps-online-public | 团辅、阵法和部分装备效果的数值交叉核对 | 只记录机制/数值参考，不声称其代码或数据受本项目 MIT 许可覆盖 |
| IcyTide/Generator | 部分装备黄字效果 ID 与数值交叉核对 | 只记录机制/数值参考，不声称其代码或数据受本项目 MIT 许可覆盖 |

## 依赖

Rust 与 Python 依赖通过 `Cargo.lock`、`Cargo.toml` 和 `python/requirements.txt` 声明，本仓库不复制其完整源码。使用、修改或再分发依赖时应遵守各依赖自身许可证。

本地语义检索使用 Apache-2.0 许可的 `fastembed-rs` 运行 ONNX 模型；默认中文向量模型为 MIT 许可的 `BAAI/bge-small-zh-v1.5`（运行时由 FastEmbed 的兼容模型仓库下载）。模型与生成的向量缓存只保存在未提交的 `backend/userdata/knowledge_index/`，不纳入本仓库源码许可。

## 公开发布门槛

将仓库转为公开前，必须至少完成以下一项：

1. 删除不具备明确公开再分发依据的客户端原始表，并证明剩余运行时数据可以合法公开；或
2. 为相应数据取得明确许可并记录来源、版本与许可证。

若发现某项内容的来源或授权记录不完整，应先停止公开分发并补充核验，而不是假定根目录 MIT 许可证能够覆盖第三方内容。
